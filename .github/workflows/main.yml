name: Chaeng Dog Image Collector

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '40 6 * * *' # 매일 오후 3시 40분 (KST) 자동 실행

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install requests python-dotenv

      - name: Run Dog Collector
        run: python lunch.py  # 파이썬 파일명이 lunch.py인 경우 그대로 둡니다.

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add dog_log.md  # .txt가 아니라 .md 파일을 추가하도록 설정!
          git commit -m "Update: 귀여운 강아지 사진 추가 🐶" || echo "No changes to commit"
          git push